bridge.log
bridge_history.db
__pycache__/
*.pyc
*.pyo
venv/
.env

# Рабочее состояние: счётчики прочитанных сообщений VK.
# Меняется при каждом опросе — в истории от него один шум,
# а при развёртывании на другой машине он вреден.
vk_catchup_state.json

# Ротированные логи
bridge.log.*
